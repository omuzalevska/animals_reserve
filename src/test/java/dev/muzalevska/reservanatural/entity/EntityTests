import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;

import java.time.LocalDate;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

public class EntityTests {

    // private Animal animal;
    // private Family family;
    // private Type type;
    // private Country country;

    // @BeforeEach
    // void setUp() {
    //     family = new Family(1L, "Felidae");
    //     type = new Type(1L, "Mammal", family);
    //     country = new Country(1L, "Kenya");
    //     animal = new Animal(1L, "Lion", family, type, Gender.MALE, country, LocalDate.of(2023, 1, 1), "lion.jpg");
    // }

    // @Test
    // void testAnimalCreation() {
    //     assertNotNull(animal);
    //     assertEquals("Lion", animal.getName());
    //     assertEquals(Gender.MALE, animal.getGender());
    //     assertEquals("Kenya", animal.getCountry().getName());
    //     assertEquals("Felidae", animal.getFamily().getName());
    //     assertEquals("Mammal", animal.getType().getName());
    // }

    // @Test
    // void testSetAnimalAttributes() {
    //     animal.setName("Tiger");
    //     animal.setGender(Gender.FEMALE);
    //     animal.setArrivalDate(LocalDate.of(2024, 1, 1));

    //     assertEquals("Tiger", animal.getName());
    //     assertEquals(Gender.FEMALE, animal.getGender());
    //     assertEquals(LocalDate.of(2024, 1, 1), animal.getArrivalDate());
    // }

    // @Test
    // void testFamilyCreation() {
    //     assertNotNull(family);
    //     assertEquals("Felidae", family.getName());
    // }

    // @Test
    // void testTypeCreation() {
    //     assertNotNull(type);
    //     assertEquals("Mammal", type.getName());
    //     assertEquals(family, type.getFamily());
    // }

    // @Test
    // void testCountryCreation() {
    //     assertNotNull(country);
    //     assertEquals("Kenya", country.getName());
    // }
}
